# Keep V1 Paper Trading Only

V1 will not execute real trades. It will scan markets, reconstruct selected history, create trade setups, simulate fills and exits, alert on notable events, and report how the strategy would have performed so execution rules can be evaluated before any trading bot is introduced.
