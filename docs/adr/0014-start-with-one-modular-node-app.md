# Start With One Modular Node App

The project will remain a single TypeScript Node app with separated modules for storage, market data, strategies, simulation, wallets, reports, and alerts. A monorepo can be extracted later when independent deployment units exist, but v1 needs a Windows-friendly local scanner and replay tool more than package-level separation.
