# Defer Live Alerting Until Discovery Is Proven

The first milestone will build the buyer discovery CLI and will not implement live alerting. Alerting requires persistent watched-wallet state, processed-block tracking, duplicate suppression, scheduling, and notification delivery, so it will be added after the V3 buyer detection model has been validated.
