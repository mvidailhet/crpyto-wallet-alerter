# Use UTC Internally And Paris For Display

The app will store timestamps in UTC and render reports and CSVs in Europe/Paris time. This matches the existing manual comparison workflow while keeping storage and scheduling resilient to daylight-saving changes.
