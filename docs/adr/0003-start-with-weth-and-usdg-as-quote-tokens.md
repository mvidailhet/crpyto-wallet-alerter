# Start With WETH And USDG As Quote Tokens

The initial pool discovery configuration will check target tokens against WETH and USDG on Robinhood Chain. These are documented Robinhood Chain token contracts and give the MVP a small, explicit discovery surface, while accepting that pools against other quote tokens will be missed until added.
